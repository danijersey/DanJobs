package com.jobpilot.job;import org.springframework.data.jpa.repository.JpaRepository;import java.util.*;public interface JobOfferRepository extends JpaRepository<JobOffer,Long>{List<JobOffer>findByUserIdOrderByCreatedAtDesc(Long userId);Optional<JobOffer>findByIdAndUserId(Long id,Long userId);boolean existsByUserIdAndExternalId(Long userId,String externalId);long countByUserId(Long userId);}
